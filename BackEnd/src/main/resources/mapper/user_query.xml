<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="com.mycom.happy.dao.UserDao">

	<insert id="userRegister" parameterType="com.mycom.happy.dto.UserDto">
		insert into users (   user_name,   user_password,   user_email,  user_profile_image_url, user_register_date ) 
		             values (  #{userName}, #{userPassword}, #{userEmail}, '', now() )
	</insert>
	
	<update id="modify" parameterType="com.mycom.happy.dto.UserDto">
		
		UPDATE   users
		   SET   user_name = #{userName},
		         user_password = #{userPassword},
		         user_email = #{userEmail}
		   WHERE user_seq = #{userSeq}
	</update>
	
	<delete id="remove" parameterType="int">
		DELETE FROM users WHERE user_seq = #{userSeq}
	</delete>
	
</mapper>